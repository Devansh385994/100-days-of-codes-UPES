#include <stdio.h>
// to check whether an integer is even or odd using if-else
int main()
{
    int n;
    printf("enter a number :");
    scanf("%d",&n);
    if(n%2==0)
{
    printf("even");
}else{printf("odd");}
return 0;
}
